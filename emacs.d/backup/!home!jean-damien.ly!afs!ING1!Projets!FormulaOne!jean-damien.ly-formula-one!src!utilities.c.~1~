#ifndef UTILITIES_H
#define UTILITIES_H

#include "control.h"

struct vector2 *get_final(struct car *car);
float scarlar_product(struct vector2 *vector, struct vector2 *vector1);
float dist(struct vector2 *vect1, struct vector2 *vect2);
float give_cost(enum flootype type, struct vector2 *vector, struct car *car);
struct float get_dist(struct vector2 *vector, struct car *car);

#endif /* !UTILITIES_H /*