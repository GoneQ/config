CC ?= gcc
CFLAGS = -std=c99 -pedantic -Wextra -Werror -Wall -g
SRCS = $(wildcard src/*.c)
OBJS = $(SRCS:.c=.o)
EXEC = myfind

all: $(OBJS)
	$(CC) $(CFLAGS) $(OBJS) -o $(EXEC)

$(OBJS): $(SRCS)

clean:
	rm $(EXEC) $(OBJS)
