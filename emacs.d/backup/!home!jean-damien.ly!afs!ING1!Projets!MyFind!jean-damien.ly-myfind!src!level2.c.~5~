#include "myfind.h"

int check_expression(struct dirent *de, int argc, char *argv[])
{
    for (int i = 1; i < argc; i++)
    {
        if (my_strcmp(argv[i], "-name") == 0)
        {
            printf("found");
        }
    }
    argv = argv;
    de = de ;
    argc = argc;
    return 1;
}