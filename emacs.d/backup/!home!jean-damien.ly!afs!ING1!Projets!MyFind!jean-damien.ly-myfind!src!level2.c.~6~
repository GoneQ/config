#include "myfind.h"

int check_expression(struct dirent *de, int argc, char *argv[])
{
    int i = 1;
    for (; (i < argc) && (argc[i][0] == '-'); i++);
    for(; i < argc)
    {
        if (my_strcmp(argv[i], "-name") == 0)
        {
            printf("found");
        }
    }
    argv = argv;
    de = de ;
    argc = argc;
    return 1;
}