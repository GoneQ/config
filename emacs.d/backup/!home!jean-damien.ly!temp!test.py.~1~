import subprocess

def test_echo_hello():
    proc = subprocess.Popen(["bash"], stdout=subprocess.PIPE,
                            stderr=subprocess.PIPE, stdin = subprocess.PIPE)
    command = str.encode("echo hello")
    out, err = proc.communicate(input=command)
    assert(out == "hello")
    